---
import "@fontsource/inter/400.css";
import "@fontsource/inter/600.css";
import "@fontsource/jetbrains-mono/400.css";
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const {
  title,
  description = "Preview TorchLens Model Menagerie gallery for rendered architecture diagrams, metadata, and dataset downloads.",
  ogImage = "/og-default.svg",
} = Astro.props;
const pageTitle = title === "TorchLens Model Menagerie" ? title : `${title} | TorchLens Model Menagerie`;
---

<!doctype html>
<html lang="en" data-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={ogImage} />
    <title>{pageTitle}</title>
  </head>
  <body>
    <div class="border-b px-4 py-2 text-center text-sm" style="border-color: var(--border); background: color-mix(in srgb, var(--accent) 14%, var(--bg));">
      Preview - rendered architecture diagrams; full validated atlas + dataset coming.
    </div>
    <header class="border-b" style="border-color: var(--border); background: color-mix(in srgb, var(--bg) 88%, transparent);">
      <nav class="shell flex min-h-16 items-center justify-between gap-4">
        <a href="/" class="mono text-sm font-semibold tracking-normal" aria-label="TorchLens Model Menagerie home">TorchLens Menagerie</a>
        <div class="flex items-center gap-2 text-sm" style="color: var(--secondary);">
          <a class="button" href="/models">Models</a>
          <a class="button" href="/dataset">Dataset</a>
          <a class="button" href="/about">About</a>
          <button id="theme-toggle" class="button" type="button" aria-label="Toggle light mode">Theme</button>
        </div>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer class="shell py-10 text-sm" style="color: var(--secondary);">
      <div class="flex flex-wrap items-center justify-between gap-3 border-t pt-6" style="border-color: var(--border);">
        <span>Preview catalog. Rendered diagrams only; validation and provenance pending.</span>
        <span class="mono">site-data/ + R2 assets</span>
      </div>
    </footer>
    <script>
      const root = document.documentElement;
      const stored = localStorage.getItem("theme");
      if (stored) root.dataset.theme = stored;
      document.getElementById("theme-toggle")?.addEventListener("click", () => {
        const next = root.dataset.theme === "light" ? "dark" : "light";
        root.dataset.theme = next;
        localStorage.setItem("theme", next);
      });
    </script>
  </body>
</html>
